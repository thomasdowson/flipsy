# ============================================================
# Shape Puzzle - Level Definitions
# ============================================================


levels <- list(
  
  # ----------------------------------------------------------
  # Shape 1: diagonal
  # 1 down, 1 right
  # ----------------------------------------------------------
  
  list(
    level = 1,
    offset = c(1, 1),
    par = 2
  ),
  
  list(
    level = 2,
    offset = c(1, 1),
    par = 3
  ),
  
  list(
    level = 3,
    offset = c(1, 1),
    par = 4
  ),
  
  list(
    level = 4,
    offset = c(1, 1),
    par = 5
  ),
  
  list(
    level = 5,
    offset = c(1, 1),
    par = 6
  ),
  
  
  # ----------------------------------------------------------
  # Shape 2: L
  # 2 down, 1 right
  # ----------------------------------------------------------
  
  list(
    level = 6,
    offset = c(2, 1),
    par = 3
  ),
  
  list(
    level = 7,
    offset = c(2, 1),
    par = 4
  ),
  
  list(
    level = 8,
    offset = c(2, 1),
    par = 5
  ),
  
  list(
    level = 9,
    offset = c(2, 1),
    par = 6
  ),
  
  list(
    level = 10,
    offset = c(2, 1),
    par = 7
  )
)